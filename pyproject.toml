[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "rgflow"
version = "0.1.0"
description = "4D SU(3) lattice gauge-theory research code"
requires-python = ">=3.10"
dependencies = ["matplotlib>=3.10", "numpy>=1.24", "scipy>=1.10"]

[project.optional-dependencies]
test = ["pytest>=8"]
train = ["torch>=2.14"]
report = ["nbformat>=5.11", "nbclient>=0.11"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
