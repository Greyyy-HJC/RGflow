# 目标

本项目旨在利用重整化群变换（renormalization-group transformations，RGTs）
加速格点场构型的生成。目前的策略将近似完美分块与条件归一化流相结合，
以实现随机逆分块。随后，通过精确的 Metropolis–Hastings（MH）更新消除流生成
构型中残留的偏差。

该框架最终应能推广到不同的格点理论和对称群，包括二维（2D）$\phi^4$ 理论、
二维 XY 模型、三维 $SU(2)$ 规范理论，以及最终的四维 $SU(3)$ 规范理论和 QCD。

## 工作流程

1. **学习一个近似分块变换。** 从线性尺寸为 $L$ 的格点上的细格构型
   $\phi_f$ 出发，应用一个平移不变、有限作用范围且可逆的核 $K$：

   $$
   \psi = K\phi_f,
   $$

   该变换保持自由度数量不变。尺度因子为 2 的下采样算子 $D$ 在二维中保留
   四个子格中的一个（例如偶-偶格点），得到

   $$
   \phi_b = D\psi = DK\phi_f.
   $$

   优化 $K$，使分块后的系综 $\{\phi_b\}$ 与在尺寸为 $L/2$ 的格点上直接
   生成的粗格系综 $\{\phi_c\}$ 尽可能匹配。在临界点处，将粗格作用量视为与
   细格作用量具有相同参数化形式，是对不动点（完美分块）关系的一种实用近似；
   精确的 RG 分块作用量通常更加复杂。

2. **参数化缺失的紫外自由度。** 将 $\phi_c$ 嵌入放大后格点的偶-偶格点上。
   对于二维中的每个 $2\times2$ 区块，引入三个细节场
   $d_{01},d_{10},d_{11}$，并用高斯隐变量 $z$ 对其进行初始化。这些场补充了
   尺度因子为 2 的分块后缺失的三个自由度。

3. **训练条件上采样流。** 训练以 $\phi_c$ 为条件的 $F_\theta$ 来生成细节场。
   一种有效的分阶段构造是先生成两个边缘子格 $d_{01}$ 和 $d_{10}$，再生成
   体心子格 $d_{11}$：

   $$
   (\phi_c,z)\xrightarrow{F_\theta}
   (\phi_c,d_{01},d_{10},d_{11})=\widetilde{\psi}.
   $$

   使 $\widetilde{\psi}$ 的局域可观测量与 $\psi=K\phi_f$ 的对应量相匹配，
   并加入对分布尾部敏感的损失项，以抑制罕见的大偏差。

4. **重建初始细格构型。** 应用逆核：

   $$
   \widetilde{\phi}_f
   =K^{-1}\widetilde{\psi}
   =K^{-1}F_\theta(\phi_c,z).
   $$

   必须显式检查 $K$ 的可逆性和数值条件，例如考察它在每个周期格点上的
   傅里叶空间谱。

5. **通过精确更新消除残留偏差。** 流只确定初始状态（第 0 次扫描）；它本身
   并不能从细格目标分布中生成精确样本。后续的粗格变量和细节变量更新必须满足
   关于细格作用量 $S_f$ 的细致平衡。

   如果粗格提议 $\phi_c\to\phi_c'$ 由一个关于 $e^{-S_c}$ 可逆的转移核生成，
   则按下式接受：

   $$
   \log A_c
   =-S_f(\phi_f')+S_f(\phi_f)+S_c(\phi_c')-S_c(\phi_c).
   $$

   依次更新 $d_{01}$、$d_{10}$ 和 $d_{11}$。对于对称的细节变量提议，

   $$
   \log A_d=-S_f(\phi_f')+S_f(\phi_f).
   $$

   在两种情况下，接受概率均为 $\min(1,e^{\log A})$。采用局域小块更新；在较大
   格点上，使用剩余类调度，使一次记录的扫描遍历所有剩余类。对于固定的小块尺寸，
   每次扫描的计算量按 $O(V)$ 标度增长。可以并行运行多条链，但并行化不能取代
   每条链内部的精确 MH 修正。

## 设计原则

核 $K$ 对细格自由度进行重组，使保留的粗格变量承载长距离结构，而互补的细节变量
主要描述短距离涨落。这样可以减轻条件流的负担，因为它只需重建缺失的紫外模式。

$K$ 的支撑范围必须足够大，以捕捉相关的红外结构；同时又要足够小，以保持局域性和
良好的体积标度。与参考研究相同，一个五参数的 $5\times5$ 核是合理的基准选择。
除分布匹配外，核的选择还应监测其可逆性、条件数以及在不同格点尺寸下的稳定性。

只有当分块系综与直接生成的粗格系综具有良好重叠时，近似分块才足够有效。任何残余
的不匹配都必须通过精确的粗格和细节 MH 更新进行修正。分块核具有局域性，因而可能
迁移到更大的体积；条件流则未必具有体积可迁移性——针对 $L=16\to32$ 训练的流，
在用于 $L=32\to64$ 时可能需要重新训练。

## 二维 $\phi^4$ 理论

离散化的格点作用量为

$$
S[\phi]
=-2\kappa\sum_{x,\mu}\phi_x\phi_{x+\hat{\mu}}
+\sum_x\left[\phi_x^2+\lambda\left(\phi_x^2-1\right)^2\right],
$$

其中，$\phi_x$ 是格点 $x$ 处的实标量场，$\hat{\mu}$ 是 $\mu$ 方向上的单位向量。
在初步的二维研究中，取 $\lambda=1.0$，并采用文献给出的临界耦合
$\kappa_{\mathrm{cr}}=0.340301$。

使用一组短距离和长距离可观测量 $\{O_i\}$ 来评估两个系综之间的重叠。一个示意性的
均值匹配损失为

$$
\mathcal{L}
=\sum_i w_i
\left(\langle O_i\rangle_A-\langle O_i\rangle_B\right)^2,
$$

其中，权重应考虑可观测量的尺度和统计不确定性。优先采用正则化的协方差加权失配：

$$
\mathcal{L}_{\mathrm{cov}}
=\Delta\boldsymbol{\mu}^{\,T}
\left(\boldsymbol{\Sigma}+\epsilon I\right)^{-1}
\Delta\boldsymbol{\mu},
\qquad
\Delta\mu_i=\langle O_i\rangle_A-\langle O_i\rangle_B,
$$

并辅以分布层面或对尾部敏感的诊断。有限组可观测量的一致性是两者相容的证据，
但不能证明两个高维分布完全相同。

### 构型生成

- 使用径向更新生成原生 $\phi^4$ 系综，格点尺寸取 $L=8,16,32$；初期目标是
  每种格点尺寸保存 1,000 个构型。
- 在选择热化舍弃长度和保存间隔之前，验证系统是否达到平衡，并估计积分自相关时间。
  报告有效样本量，因为保存的 1,000 个构型并不一定等同于 1,000 个独立样本。
- 将统计独立的链或间隔充分大的构型划分为训练集、验证集和测试集。
- 初始上采样研究采用 $L=8\to16$ 和 $L=16\to32$ 两组尺寸。仅在测试
  $L=32\to64$ 时加入原生 $L=64$ 构型。

### 分块核训练

- 从平移不变的 $5\times5$ 核 $K$ 开始，选定一个固定的下采样算子 $D$，
  并将 $\{DK\phi_f\}$ 与原生粗格系综 $\{\phi_c\}$ 进行匹配。
- 使用协方差加权的局域可观测量匹配进行训练，然后在留出数据上验证作用量密度、
  磁化强度、两点关联函数、磁化率、关联长度和 Binder 累积量的分布。
- 在训练或推断所使用的每一种格点尺寸上，检查 $K$ 的谱和条件数。

### 条件流训练

- 以嵌入后的粗格场为条件，分阶段生成三个细节子格。
- 使用局域可观测量损失和对尾部敏感的损失，将提议的变换场
  $F_\theta(\phi_c,z)=\widetilde{\psi}$ 与 $\psi=K\phi_f$ 进行匹配。
- 在留出数据上比较原生细格系综和初始上采样细格系综。监测分布重叠，而不仅是
  均值匹配。

### 重建与精确采样

- 构造初始细格提议
  $\widetilde{\phi}_f=K^{-1}F_\theta(\phi_c,z)$。
- 使用上述精确的粗格 MH 更新和依次进行的细节 MH 更新，对每个提议重新热化。
- 跟踪可观测量随记录扫描次数的变化；只有当其分布在不确定性范围内与原生细格系综
  一致后，才选择一个固定的生产扫描次数。
- 通过测量接受率、重新热化时间、积分自相关时间、单位成本下的有效样本量以及随体积
  的标度关系，量化该方法相对于直接上采样的优势。

预期优势在于，条件流只需学习缺失的紫外结构，而不必学习完整的细格分布。近似分块
步骤将已达到平衡的红外模式传递到细格点，而精确的局域 MH 更新则比全体积接受/拒绝
步骤更高效地修正剩余偏差。
